import React from 'react';
import { Link } from '@reach/router';

const Header = () => (
  /* eslint-disable */
  <div>
    <Link to="/">Home</Link>
    <Link to="/roster">Dashboard</Link>
  </div>
  /* eslint-disable */
);

export default Header;
